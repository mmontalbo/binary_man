#!/usr/bin/env bash
set -euo pipefail

root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
bin="$root/target/debug/bgrid"

if [[ ! -x "$bin" ]]; then
  (cd "$root" && cargo build --bin bgrid)
fi

exec "$bin" "$@"
