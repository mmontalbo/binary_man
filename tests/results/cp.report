# Exploration: cp

## Rounds
  round 0: 4 isolated cumulative, 1 identical in round (discovery)
  round 1: 5 isolated cumulative, 2 identical in round (refinement)
  round 2: 6 isolated cumulative, 3 identical in round (refinement)
  round 3: 17 isolated cumulative, 3 identical in round (refinement)

## Distinguished: 36/36 flags
  0 observed behavior (0 solo, 0 via combination)
  36 error-differentiated only (3 solo, 33 via combination)

Aliases: -L = --dereference, -P = --no-dereference, -S = --suffix, -T = --no-target-directory, -a = --archive, -f = --force, -i = --interactive, -l = --link, -n = --no-clobber, -s = --symbolic-link, -t = --target-directory, -v = --verbose, -x = --one-file-system

## Solo (unique behavior)
  --no-preserve  # don't preserve the specified attributes
    exemplar: "--no-preserve=attr_list" in numeric_standard / env COLUMNS=40 (different, exit 1→127)
    base:
      (no base)
    flag:
  -S  # override the usual backup suffix
    exemplar: "-S" in numeric_standard / env COLUMNS=40 (different, exit 1→127)
    base:
      (no base)
    flag:
  -t  # copy all SOURCE arguments into DIRECTORY
    exemplar: "-t" in numeric_standard / env COLUMNS=40 (different, exit 1→127)
    base:
      (no base)
    flag:

## Via combination (distinguishable when paired)
  --attributes-only  e.g. (pairwise evidence)
  --backup  e.g. (pairwise evidence)
  --context  e.g. (pairwise evidence)
  --copy-contents  e.g. (pairwise evidence)
  --debug  e.g. (pairwise evidence)
  --keep-directory-symlink  e.g. (pairwise evidence)
  --parents  e.g. (pairwise evidence)
  --preserve  e.g. (pairwise evidence)
  --recursive  e.g. (pairwise evidence)
  --reflink  e.g. (pairwise evidence)
  --remove-destination  e.g. (pairwise evidence)
  --sparse  e.g. "-b" "-n" "--sparse" "-P"
  --strip-trailing-slashes  e.g. (pairwise evidence)
  --update  e.g. (pairwise evidence)
  -H  e.g. (pairwise evidence)
  -L  e.g. "-b" "-n" "--sparse" "-L"
  -P  e.g. "-b" "-n" "--sparse" "-P"
  -R  e.g. "-b" "-n" "--sparse" "-R"
  -T  e.g. "-b" "-n" "--sparse" "-T"
  -Z  e.g. "-b" "-n" "--sparse" "-Z"
  -a  e.g. (pairwise evidence)
  -b  e.g. "-b" "-n" "--sparse" "-P"
  -d  e.g. (pairwise evidence)
  -f  e.g. (pairwise evidence)
  -i  e.g. (pairwise evidence)
  -l  e.g. "-b" "-n" "--sparse" "-l"
  -n  e.g. "-b" "-n" "--sparse" "-P"
  -p  e.g. "-b" "-n" "--sparse" "-p"
  -r  e.g. "-b" "-n" "--sparse" "-r"
  -s  e.g. "-b" "-n" "--sparse" "-s"
  -u  e.g. "-b" "-n" "--sparse" "-u"
  -v  e.g. (pairwise evidence)
  -x  e.g. "-b" "-n" "--sparse" "-x"

