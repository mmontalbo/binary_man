# Exploration: cp

## Rounds
  round 0: 34 isolated cumulative, 11 identical in round (discovery)
  round 1: 39 isolated cumulative, 19 identical in round (refinement)
  round 2: 54 isolated cumulative, 25 identical in round (refinement)
  round 3: 66 isolated cumulative, 26 identical in round (refinement)

## Observed: 34/36 flags
  20 solo, 14 via combination
  2 additional error-differentiated

Aliases: -L = --dereference, -P = --no-dereference, -S = --suffix, -T = --no-target-directory, -a = --archive, -f = --force, -i = --interactive, -l = --link, -n = --no-clobber, -s = --symbolic-link, -t = --target-directory, -v = --verbose, -x = --one-file-system

## Solo (unique behavior)
  --attributes-only  # don't copy the file data, just the attributes
    exemplar: "--attributes-only" "input.txt" "other.txt" in many_files /remove subdir (different)
    base:
      (no base)
    flag:
  --backup
    exemplar: "--backup" "input.txt" "subdir" in alpha_standard / content_case_upper (different)
    base:
      (no base)
    flag:
  --debug  # explain how a file is copied.  Implies -v
    exemplar: "--debug" "input.txt" "other.txt" in many_files /remove subdir (different)
    base:
      (no base)
    flag:
      'input.txt' -> 'other.txt'
      copy offload: yes, reflink: unsupported, sparse detection: no
  --keep-directory-symlink  # follow existing symlinks to directories
    exemplar: "--keep-directory-symlink" "input.txt" "subdir" in alpha_standard / content_case_upper (different)
    base:
      (no base)
    flag:
  --no-preserve  # don't preserve the specified attributes
    exemplar: "--no-preserve=attr_list" "input.txt" "other.txt" in many_files /remove subdir (different)
    base:
      (no base)
    flag:
  --parents  # use full source file name under DIRECTORY
    exemplar: "--parents" "input.txt" "other.txt" in many_files /remove subdir (different)
    base:
      (no base)
    flag:
  --recursive
    exemplar: "--recursive" "input.txt" "subdir" in alpha_standard / content_case_upper (different)
    base:
      (no base)
    flag:
  --reflink
    exemplar: "--reflink" "input.txt" "subdir" in many_files /remove subdir (different)
    base:
      (no base)
    flag:
  --remove-destination  # remove each existing destination file before
    exemplar: "--remove-destination" "input.txt" "subdir" in alpha_standard / content_case_upper (different)
    base:
      (no base)
    flag:
  --sparse  # control creation of sparse files. See below
    exemplar: "--sparse=auto" "input.txt" "subdir" in alpha_standard / content_case_upper (different)
    base:
      (no base)
    flag:
  --update
    exemplar: "--update" "input.txt" "subdir" in alpha_standard / content_case_upper (different)
    base:
      (no base)
    flag:
  -L  # always follow symbolic links in SOURCE
    exemplar: "-L" "input.txt" "subdir" in alpha_standard / content_case_upper (different)
    base:
      (no base)
    flag:
  -S  # override the usual backup suffix
    exemplar: "-S" "input.txt" "subdir" in many_files /remove subdir (different)
    base:
      (no base)
    flag:
  -T  # treat DEST as a normal file
    exemplar: "-T" "input.txt" "other.txt" in many_files /remove subdir (different)
    base:
      (no base)
    flag:
  -Z  # set SELinux security context of destination
    exemplar: "-Z" "input.txt" "subdir" in alpha_standard / content_case_upper (different)
    base:
      (no base)
    flag:
  -b  # like --backup but does not accept an argument
    exemplar: "-b" "input.txt" "subdir" in alpha_standard / content_case_upper (different)
    base:
      (no base)
    flag:
  -f  # if an existing destination file cannot be
    exemplar: "-f" "input.txt" "subdir" in alpha_standard / content_case_upper (different)
    base:
      (no base)
    flag:
  -i  # prompt before overwrite (overrides a previous -n
    exemplar: "-i" "input.txt" "subdir" in alpha_standard / content_case_upper (different)
    base:
      (no base)
    flag:
  -l  # hard link files instead of copying
    exemplar: "-l" "input.txt" "subdir" in alpha_standard / content_case_upper (different)
    base:
      (no base)
    flag:
  -s  # make symbolic links instead of copying
    exemplar: "-s" "input.txt" "other.txt" in many_files /remove subdir (different)
    base:
      (no base)
    flag:
  -t  # copy all SOURCE arguments into DIRECTORY
    exemplar: "-t" "input.txt" "subdir" in many_files /remove subdir (different)
    base:
      (no base)
    flag:
  -v  # explain what is being done
    exemplar: "-v" "input.txt" "other.txt" in many_files /remove subdir (different)
    base:
      (no base)
    flag:
      'input.txt' -> 'other.txt'

## Via combination (distinguishable when paired)
  --context  e.g. "--debug" "--context" "input.txt" "other.txt"
  --copy-contents  e.g. "--remove-destination" "--copy-contents" "input.txt" "other.txt"
  --preserve  e.g. "--backup" "--preserve" "-u" "input.txt" "other.txt"
  --strip-trailing-slashes  e.g. "--backup" "--strip-trailing-slashes" "input.txt" "other.txt"
  -H  e.g. (pairwise evidence)
  -P  e.g. (pairwise evidence)
  -R  e.g. (pairwise evidence)
  -a  e.g. (pairwise evidence)
  -d  e.g. (pairwise evidence)
  -n  e.g. "-b" "-n" "input.txt" "subdir"
  -p  e.g. "--debug" "--backup" "-p" "input.txt" "other.txt"
  -r  e.g. (pairwise evidence)
  -u  e.g. "--backup" "--preserve" "-u" "input.txt" "other.txt"
  -x  e.g. (pairwise evidence)

