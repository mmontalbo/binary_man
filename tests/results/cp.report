# Exploration: cp

## Rounds
  round 0: 34 isolated cumulative, 11 identical in round (discovery)
  round 1: 36 isolated cumulative, 24 identical in round (refinement)
  round 2: 43 isolated cumulative, 34 identical in round (refinement)
  round 3: 53 isolated cumulative, 33 identical in round (refinement)

## Observed: 31/36 flags
  12 solo, 19 via combination
  3 additional error-differentiated
  1 behavioral aliases detected
  2 indistinguishable under tested conditions

Aliases: -L = --dereference, -P = --no-dereference, -S = --suffix, -T = --no-target-directory, -a = --archive, -f = --force, -i = --interactive, -l = --link, -n = --no-clobber, -s = --symbolic-link, -t = --target-directory, -v = --verbose, -x = --one-file-system

## Solo (unique behavior)
  --attributes-only  # don't copy the file data, just the attributes
    exemplar: "--attributes-only" "input.txt" "other.txt" in alpha_minimal (different)
    base:
    flag:
  --backup
    exemplar: "--backup" "input.txt" "other.txt" in alpha_minimal (different)
    base:
    flag:
  --debug  # explain how a file is copied.  Implies -v
    exemplar: "--debug" "input.txt" "other.txt" in alpha_minimal (different)
    base:
    flag:
      'input.txt' -> 'other.txt'
      copy offload: yes, reflink: unsupported, sparse detection: no
  --no-preserve  # don't preserve the specified attributes
    exemplar: "--no-preserve=attr_list" "input.txt" "subdir" in alpha_minimal (different)
    base:
    flag:
  --parents  # use full source file name under DIRECTORY
    exemplar: "--parents" "input.txt" "subdir" in alpha_minimal (different)
    base:
    flag:
  --reflink
    exemplar: "--reflink" "input.txt" "other.txt" in alpha_minimal (different)
    base:
    flag:
  --remove-destination  # remove each existing destination file before
    exemplar: "--remove-destination" "input.txt" "subdir" in alpha_minimal (different)
    base:
    flag:
  -S  # override the usual backup suffix
    exemplar: "-S" "input.txt" "subdir" in alpha_minimal (different)
    base:
    flag:
  -T  # treat DEST as a normal file
    exemplar: "-T" "input.txt" "other.txt" in alpha_minimal (different)
    base:
    flag:
  -b  # like --backup but does not accept an argument
    exemplar: "-b" "input.txt" "subdir" in alpha_minimal (different)
    base:
    flag:
  -i  # prompt before overwrite (overrides a previous -n
    exemplar: "-i" "input.txt" "subdir" in alpha_minimal (different)
    base:
    flag:
  -l  # hard link files instead of copying
    exemplar: "-l" "input.txt" "subdir" in alpha_minimal (different)
    base:
    flag:
  -s  # make symbolic links instead of copying
    exemplar: "-s" "input.txt" "other.txt" in alpha_minimal (different)
    base:
    flag:
  -t  # copy all SOURCE arguments into DIRECTORY
    exemplar: "-t" "input.txt" "subdir" in alpha_minimal (different)
    base:
    flag:
  -v  # explain what is being done
    exemplar: "-v" "input.txt" "subdir" in alpha_minimal (reordered)
    base:
    flag:
      'input.txt' -> 'subdir'

## Via combination (distinguishable when paired)
  --context  e.g. "--suffix=suffix" "--context" "input.txt" "other.txt"
  --copy-contents  e.g. (pairwise evidence)
  --keep-directory-symlink  e.g. "--remove-destination" "--keep-directory-symlink" "input.txt" "other.txt"
  --preserve  e.g. "--reflink" "--preserve" "input.txt" "other.txt"
  --recursive  e.g. "--target-directory=." "--recursive" "input.txt" "subdir"
  --sparse  e.g. (pairwise evidence)
  --strip-trailing-slashes  e.g. (pairwise evidence)
  --update  e.g. "--backup" "--update" "input.txt" "other.txt"
  -H  e.g. (pairwise evidence)
  -L  e.g. (pairwise evidence)
  -P  e.g. (pairwise evidence)
  -R  e.g. (pairwise evidence)
  -Z  e.g. (pairwise evidence)
  -a  e.g. (pairwise evidence)
  -d  e.g. (pairwise evidence)
  -f  e.g. (pairwise evidence)
  -n  e.g. "-b" "-n" "input.txt" "subdir"
  -p  e.g. "--suffix=suffix" "--backup" "-p" "input.txt" "other.txt"
  -r  e.g. (pairwise evidence)

## Indistinguishable under tested conditions
  UNEXPLAINED (2): -u, -x
  UNEXPLAINED (2): -u, -x

