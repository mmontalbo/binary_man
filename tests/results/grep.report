# Exploration: grep

## Rounds
  round 0: 39 isolated cumulative, 17 identical in round (discovery)
  round 1: 59 isolated cumulative, 26 identical in round (refinement)
  round 2: 67 isolated cumulative, 35 identical in round (refinement)
  round 3: 75 isolated cumulative, 39 identical in round (refinement)

## Observed: 34/49 flags
  17 solo, 17 via combination
  6 additional error-differentiated
  1 behavioral aliases detected
  8 indistinguishable under tested conditions

Aliases: -A = --after-context, -B = --before-context, -C = --context, -D = --devices, -E = --extended-regexp, -F = --fixed-strings, -G = --basic-regexp, -H = --with-filename, -L = --files-without-match, -P = --perl-regexp, -R = --dereference-recursive, -T = --initial-tab, -U = --binary, -Z = --null, -a = --text, -b = --byte-offset, -c = --count, -d = --directories, -e = --regexp, -f = --file, -h = --no-filename, -i = --ignore-case, -l = --files-with-matches, -m = --max-count, -n = --line-number, -o = --only-matching, -r = --recursive, -s = --no-messages, -v = --invert-match, -w = --word-regexp, -x = --line-regexp, -z = --null-data

## Solo (unique behavior)
  --binary-files  # assume that binary files are TYPE;
  --include  # search only files that match GLOB (a file pattern)
    exemplar: "--include=glob" "$(head -n1 input.txt)" "input.txt" in many_files /remove env LC_ALL (different)
    base:
      (no base)
    flag:
  -A  # print NUM lines of trailing context
    exemplar: "-A" "$(head -n1 input.txt)" "input.txt" in many_files /remove env LC_ALL (different)
    base:
      (no base)
    flag:
  -B  # print NUM lines of leading context
    exemplar: "-B" "$(head -n1 input.txt)" "input.txt" in many_files /remove env LC_ALL (different)
    base:
      (no base)
    flag:
  -C  # print NUM lines of output context
    exemplar: "-C" "$(head -n1 input.txt)" "input.txt" in many_files /remove env LC_ALL (different)
    base:
      (no base)
    flag:
  -D  # how to handle devices, FIFOs and sockets;
  -H  # print file name with output lines
    exemplar: "-H" "$(head -n1 input.txt)" "input.txt" in many_files /remove env LC_ALL (different)
    base:
      (no base)
    flag:
      input.txt:100
      input.txt:1000
  -L  # print only names of FILEs with no selected lines
    exemplar: "-L" "$(head -n1 input.txt)" "input.txt" in numeric_standard / input.txt=empty (+1 lines, exit 0→1)
    base:
    flag:
      input.txt
  -V  # display version information and exit
    exemplar: "-V" "$(head -n1 input.txt)" "input.txt" in many_files /input.txt=size:1 (different)
    base:
      (no base)
    flag:
      grep (GNU grep) 3.11
      Copyright (C) 2023 Free Software Foundation, Inc.
      License GPLv3+: GNU GPL version 3 or later <https://gnu.org/licenses/gpl.html>.
      This is free software: you are free to change and redistribute it.
      There is NO WARRANTY, to the extent permitted by law.
            ...
  -b  # print the byte offset with output lines
    exemplar: "-b" "$(head -n1 input.txt)" "input.txt" in many_files /remove env LC_ALL (different)
    base:
      (no base)
    flag:
      0:100
      39:1000
  -c  # print only a count of selected lines per FILE
    exemplar: "-c" "$(head -n1 input.txt)" "input.txt" in many_files /input.txt=size:1 (different)
    base:
      (no base)
    flag:
      1
  -d  # how to handle directories;
    exemplar: "-d" "$(head -n1 input.txt)" "input.txt" in many_files /remove env LC_ALL (different)
    base:
      (no base)
    flag:
  -e  # use PATTERNS for matching
    exemplar: "-e" "$(head -n1 input.txt)" "input.txt" in many_files /remove env LC_ALL (different)
    base:
      (no base)
    flag:
      100
      1000
  -f  # take PATTERNS from FILE
  -l  # print only names of FILEs with selected lines
    exemplar: "-l" "$(head -n1 input.txt)" "input.txt" in many_files /input.txt=size:1 (different)
    base:
      (no base)
    flag:
      input.txt
  -m  # stop after NUM selected lines
    exemplar: "-m" "$(head -n1 input.txt)" "input.txt" in many_files /remove env LC_ALL (different, exit 2→1)
    base:
      (no base)
    flag:
  -n  # print line number with output lines
    exemplar: "-n" "$(head -n1 input.txt)" "input.txt" in many_files /remove env LC_ALL (different)
    base:
      (no base)
    flag:
      1:100
      15:1000
  -o  # show only nonempty parts of lines that match
    exemplar: "-o" "$(head -n1 input.txt)" "input.txt" in many_files /remove env LC_ALL (different)
    base:
      (no base)
    flag:
      100
      100
  -s  # suppress error messages
    exemplar: "-s" "$(head -n1 input.txt)" "input.txt" in many_files /remove env LC_ALL (different)
    base:
      (no base)
    flag:
      100
      1000
  -v  # select non-matching lines
    exemplar: "-v" "$(head -n1 input.txt)" "input.txt" in many_files /remove env LC_ALL (different)
    base:
      (no base)
    flag:
      2
      30
      1
      20
      3
            ...
  -z  # a data line ends in 0 byte, not newline
    exemplar: "-z" "$(head -n1 input.txt)" "input.txt" in many_files /remove env LC_ALL (different)
    base:
      (no base)
    flag:
      100
      2
      30
      1
      20
            ...

## Via combination (distinguishable when paired)
  --color  e.g. "-d" "--color"
  --colour  e.g. "-f" "--colour" "$(head -n1 input.txt)" "input.txt"
  --exclude  e.g. "--file=input.txt" "--exclude"
  --exclude-dir  e.g. "--regexp=patterns" "--exclude-dir"
  --exclude-from  e.g. "-d" "--exclude-from" "$(head -n1 input.txt)" "input.txt"
  --group-separator  e.g. "-d" "--group-separator" "$(head -n1 input.txt)" "input.txt"
  --label  e.g. "-d" "--label" "$(head -n1 input.txt)" "input.txt"
  --line-buffered  e.g. "-d" "--line-buffered"
  --no-group-separator  e.g. "-d" "--no-group-separator" "$(head -n1 input.txt)" "input.txt"
  --no-ignore-case  e.g. "-d" "--no-ignore-case"
  --quiet  e.g. "-d" "--quiet"
  --silent  e.g. "-d" "--silent"
  -E  e.g. (pairwise evidence)
  -F  e.g. (pairwise evidence)
  -G  e.g. (pairwise evidence)
  -P  e.g. (pairwise evidence)
  -a  e.g. (pairwise evidence)
  -h  e.g. (pairwise evidence)
  -i  e.g. (pairwise evidence)

## Indistinguishable under tested conditions
  UNEXPLAINED (6): -I, -R, -T, -U, -Z, -r
  UNEXPLAINED (2): -w, -x

