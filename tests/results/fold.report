# Exploration: fold

## Rounds
  round 0: 12 isolated cumulative, 22 identical in round (single-phase)

## Observed: 3/3 flags
  1 solo, 2 via combination

Aliases: -b = --bytes, -s = --spaces, -w = --width

## Solo (unique behavior)
  -w  # use WIDTH columns instead of 80
    exemplar: "-w" in numeric_minimal (different)
    base:
    flag:

## Via combination (distinguishable when paired)
  -b  e.g. (pairwise evidence)
  -s  e.g. (pairwise evidence)

