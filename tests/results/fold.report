# Exploration: fold

## Rounds
  round 0: 13 isolated cumulative, 26 identical in round (single-phase)

## Observed: 3/3 flags
  1 solo, 2 via combination

Aliases: -b = --bytes, -s = --spaces, -w = --width

## Solo (unique behavior)
  -w  # use WIDTH columns instead of 80
    exemplar: "-w" in numbers_minimal (different)
    base:
    flag:

## Via combination (distinguishable when paired)
  -b  e.g. "-b" "-s" "$(head -n1 input.txt)" "input.txt"
  -s  e.g. "-b" "-s" "$(head -n1 input.txt)" "input.txt"

