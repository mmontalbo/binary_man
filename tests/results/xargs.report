# Exploration: xargs

## Rounds
  round 0: 5 isolated cumulative, 13 identical in round (discovery)
  round 1: 30 isolated cumulative, 17 identical in round (refinement)
  round 2: 47 isolated cumulative, 27 identical in round (refinement)
  round 3: 47 isolated cumulative, 23 identical in round (refinement)

## Observed: 14/21 flags
  3 solo, 11 via combination
  7 additional error-differentiated
  1 behavioral aliases detected

Aliases: -0 = --null, -L = --max-lines, -P = --max-procs, -a = --arg-file, -d = --delimiter, -n = --max-args, -o = --open-tty, -p = --interactive, -r = --no-run-if-empty, -s = --max-chars, -t = --verbose, -x = --exit

## Solo (unique behavior)
  --show-limits  # show limits on command-line length
    exemplar: "--show-limits" in alpha_minimal (different)
    base:
    flag:
      cherry apple banana
  -a  # read arguments from FILE, not standard input
    exemplar: "-a" in many_files /remove env LC_ALL (different)
    base:
      (no base)
    flag:
  -o  # Reopen stdin as /dev/tty in the child process
  -t  # print commands before executing them
    exemplar: "-t" in alpha_minimal (different)
    base:
    flag:
      cherry apple banana

## Via combination (distinguishable when paired)
  --eof  e.g. (pairwise evidence)
  --process-slot-var  e.g. "--show-limits" "--process-slot-var"
  --replace  e.g. (pairwise evidence)
  -0  e.g. (pairwise evidence)
  -E  e.g. "-a" "-E"
  -I  e.g. "-a" "-I"
  -L  e.g. "-L" "-s"
  -P  e.g. "-a" "-P"
  -d  e.g. "-d" "-E"
  -e  e.g. "-L" "-e"
  -i  e.g. "--show-limits" "-i"
  -l  e.g. (pairwise evidence)
  -n  e.g. "-a" "-n"
  -p  e.g. "--show-limits" "-p"
  -r  e.g. "-L" "-r"
  -s  e.g. "-L" "-s"
  -x  e.g. "-n" "-x"

