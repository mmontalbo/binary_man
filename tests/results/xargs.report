# Exploration: xargs

## Rounds
  round 0: 7 isolated cumulative, 26 identical in round (discovery)
  round 1: 33 isolated cumulative, 8 identical in round (refinement)
  round 2: 34 isolated cumulative, 8 identical in round (refinement)
  round 3: 36 isolated cumulative, 8 identical in round (refinement)

## Observed: 1/21 flags
  1 solo, 0 via combination
  20 additional error-differentiated
  1 behavioral aliases detected

Aliases: -0 = --null, -L = --max-lines, -P = --max-procs, -a = --arg-file, -d = --delimiter, -n = --max-args, -o = --open-tty, -p = --interactive, -r = --no-run-if-empty, -s = --max-chars, -t = --verbose, -x = --exit

## Solo (unique behavior)
  --show-limits  # show limits on command-line length
    exemplar: "--show-limits" in alpha_minimal (different)
    base:
    flag:
  -E
    exemplar: "-E" in alpha_minimal (different)
    base:
    flag:
  -I
    exemplar: "-I" in alpha_minimal (different)
    base:
    flag:
  -L  # use at most MAX-LINES non-blank input lines per
    exemplar: "-L" in alpha_minimal (different)
    base:
    flag:
  -P  # run at most MAX-PROCS processes at a time
    exemplar: "-P" in alpha_minimal (different)
    base:
    flag:
  -a  # read arguments from FILE, not standard input
    exemplar: "-a" in alpha_minimal (different)
    base:
    flag:
  -d  # items in input stream are separated by CHARACTER,
    exemplar: "-d" in alpha_minimal (different)
    base:
    flag:
  -n  # use at most MAX-ARGS arguments per command line
    exemplar: "-n" in alpha_minimal (different)
    base:
    flag:
  -o  # Reopen stdin as /dev/tty in the child process
  -p  # prompt before running commands
    exemplar: "-p" in alpha_minimal (different)
    base:
    flag:
  -s  # limit length of command line to MAX-CHARS
    exemplar: "-s" in alpha_minimal (different)
    base:
    flag:
  -t  # print commands before executing them
    exemplar: "-t" in alpha_minimal (different)
    base:
    flag:

## Via combination (distinguishable when paired)
  --eof  e.g. "--show-limits" "--eof"
  --process-slot-var  e.g. "--show-limits" "--process-slot-var"
  --replace  e.g. "--show-limits" "--replace"
  -0  e.g. (pairwise evidence)
  -e  e.g. "--show-limits" "-e"
  -i  e.g. (pairwise evidence)
  -l  e.g. (pairwise evidence)
  -r  e.g. (pairwise evidence)
  -x  e.g. (pairwise evidence)

