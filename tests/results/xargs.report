# Exploration: xargs

## Rounds
  round 0: 3 isolated cumulative, 20 identical in round (single-phase)

## Observed: 14/21 flags
  1 solo, 13 via combination
  7 additional error-differentiated

Aliases: -0 = --null, -L = --max-lines, -P = --max-procs, -a = --arg-file, -d = --delimiter, -n = --max-args, -o = --open-tty, -p = --interactive, -r = --no-run-if-empty, -s = --max-chars, -t = --verbose, -x = --exit

## Solo (unique behavior)
  --show-limits  # show limits on command-line length
    exemplar: "--show-limits" in words_minimal (different)
    base:
    flag:
      cherry apple banana

## Via combination (distinguishable when paired)
  --eof  e.g. (pairwise evidence)
  --process-slot-var  e.g. (pairwise evidence)
  --replace  e.g. (pairwise evidence)
  -0  e.g. (pairwise evidence)
  -E  e.g. (pairwise evidence)
  -I  e.g. (pairwise evidence)
  -L  e.g. (pairwise evidence)
  -P  e.g. (pairwise evidence)
  -a  e.g. (pairwise evidence)
  -d  e.g. (pairwise evidence)
  -e  e.g. (pairwise evidence)
  -i  e.g. (pairwise evidence)
  -l  e.g. (pairwise evidence)
  -n  e.g. (pairwise evidence)
  -o  e.g. (pairwise evidence)
  -p  e.g. (pairwise evidence)
  -r  e.g. (pairwise evidence)
  -s  e.g. (pairwise evidence)
  -t  e.g. (pairwise evidence)
  -x  e.g. (pairwise evidence)

