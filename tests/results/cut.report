# Exploration: cut

## Rounds
  round 0: 0 isolated cumulative, 13 identical in round (single-phase)

## Observed: 3/10 flags
  0 solo, 3 via combination
  6 additional error-differentiated
  robustness: 9 robust, 0 moderate, 0 fragile

Aliases: -b = --bytes, -c = --characters, -d = --delimiter, -f = --fields, -s = --only-delimited, -z = --zero-terminated

## Solo (unique behavior)

## Via combination (distinguishable when paired)
  --complement  e.g. (pairwise evidence)
  --output-delimiter  e.g. (pairwise evidence)
  -M  e.g. (pairwise evidence)
  -b  e.g. (pairwise evidence)
  -c  e.g. (pairwise evidence)
  -f  e.g. (pairwise evidence)
  -n  e.g. (pairwise evidence)
  -s  e.g. (pairwise evidence)
  -z  e.g. (pairwise evidence)

