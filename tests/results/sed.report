# Exploration: sed

## Rounds
  round 0: 21 isolated cumulative, 71 identical in round (single-phase)

## Observed: 23/24 flags
  3 solo, 20 via combination
  1 additional error-differentiated
  3 behavioral aliases detected

## Solo (unique behavior)
  --debug
    exemplar: "--debug" "input.txt" "other.txt" in words_minimal (-7 lines)
    base:
      nput.txt
      hello world
    flag:
      SED PROGRAM:
        i\nput.txt
      
      INPUT:   'other.txt' line 1
      PATTERN: hello world
            ...
  --follow-symlinks
    exemplar: "--follow-symlinks" "input.txt" "other.txt" in words_minimal (-2 lines)
    base:
      nput.txt
      hello world
    flag:
      nput.txt
      hello world
  --posix
    exemplar: "--posix" "$(head -n1 input.txt)" "input.txt" in passwd_minimal (+30 lines, exit 1→0)
    base:
      root:x:0:0:root:/root:/bin/bash
      daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin
      bin:x:2:2:bin:/bin:/usr/sbin/nologin
      sys:x:3:3:sys:/dev:/usr/sbin/nologin
      sync:x:4:65534:sync:/bin:/bin/sync
            ...
    flag:
      root:x:0:0:root:/root:/bin/bash
      daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin
      bin:x:2:2:bin:/bin:/usr/sbin/nologin
      sys:x:3:3:sys:/dev:/usr/sbin/nologin
      sync:x:4:65534:sync:/bin:/bin/sync
            ...
  -l
    exemplar: "-l" "input.txt" in words_minimal (different)
    base:
    flag:

## Via combination (distinguishable when paired)
  --expression  e.g. (pairwise evidence)
  --file  e.g. "-f" "--file" "$(head -n1 input.txt)" "input.txt"
  --in-place  e.g. "--in-place" "--sandbox" "$(head -n1 input.txt)" "input.txt"
  --line-length  e.g. (pairwise evidence)
  --null-data  e.g. "-i" "--null-data" "$(head -n1 input.txt)" "input.txt"
  --quiet  e.g. (pairwise evidence)
  --regexp-extended  e.g. (pairwise evidence)
  --sandbox  e.g. "-i" "--sandbox" "$(head -n1 input.txt)" "input.txt"
  --separate  e.g. (pairwise evidence)
  --silent  e.g. (pairwise evidence)
  --unbuffered  e.g. (pairwise evidence)
  -E  e.g. (pairwise evidence)
  -e  e.g. (pairwise evidence)
  -f  e.g. "-f" "--file" "$(head -n1 input.txt)" "input.txt"
  -i  e.g. "-i" "--follow-symlinks" "$(head -n1 input.txt)" "input.txt"
  -n  e.g. (pairwise evidence)
  -r  e.g. (pairwise evidence)
  -s  e.g. (pairwise evidence)
  -u  e.g. (pairwise evidence)
  -z  e.g. "-i" "-z" "$(head -n1 input.txt)" "input.txt"

