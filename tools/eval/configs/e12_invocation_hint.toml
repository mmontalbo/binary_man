name = "exp_e12_invocation_hint"
parallel_groups = true

[defaults]
runs = 3
max_cycles = 80

[[groups]]
lms = ["claude:haiku"]
binaries = [["ls"], ["grep"], ["git", "diff"]]
timeout = 600

[[groups]]
lms = ["ollama:qwen3-coder:30b"]
binaries = [["ls"], ["grep"]]
timeout = 1800
